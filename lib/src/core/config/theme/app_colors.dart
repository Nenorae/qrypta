// lib/src/core/config/theme/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // --- PALET WARNA BARU ---
  // Berdasarkan gambar yang Anda berikan.

  /// Warna latar belakang utama (hijau sangat gelap)
  static const Color background = Color(0xFF032221);

  /// Warna primer untuk elemen interaktif (hijau)
  static const Color primary = Color(0xFF03624C);

  /// Warna aksen, digunakan untuk highlight atau logo.
  /// Kita gunakan warna primer juga untuk konsistensi.
  static const Color accent = Color(0xFF03624C);

  /// Warna font utama (putih gading)
  static const Color font = Color(0xFFEFE9E7);

  // Warna lain bisa tetap sama atau disesuaikan nanti
  static const Color error = Colors.redAccent;
}
